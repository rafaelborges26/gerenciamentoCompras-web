import styled from 'styled-components'

export const ContainerButton = styled.button`
    
        font-size: 24px;
        background: red;

        > button {
            gap: 10px;
        } 
    
`